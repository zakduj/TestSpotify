services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: testspotify-app
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    restart: unless-stopped